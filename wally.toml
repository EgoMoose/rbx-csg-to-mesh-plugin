[package]
name = "egomoose/rbx-csg-to-mesh-plugin"
description = "A roblox plugin that converts union operations to editable meshes."
repository = "https://github.com/EgoMoose/rbx-csg-to-mesh-plugin"
version = "0.1.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"
license = "MIT"
exclude = ["**"]
include = ["src", "src/**", "wally.toml", "wally.lock", "default.project.json", "LICENSE"]
private = true